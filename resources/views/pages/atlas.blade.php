<x-layouts.app title="Opportunity Atlas"><section class="mx-auto max-w-6xl px-6 py-16"><h1 class="text-4xl font-bold">Opportunity Atlas</h1>@foreach($industries as $industry)<h2 class="mt-10 text-2xl font-semibold">{{ $industry->name }}</h2><div class="mt-4 grid gap-5 md:grid-cols-2">@foreach($industry->workflows as $workflow)<x-card><h3 class="text-xl font-semibold">{{ $workflow->name }}</h3><p class="text-slate-300">{{ $workflow->description }}</p>@foreach($workflow->frictionPoints as $fp)<div class="mt-4 border-t border-white/10 pt-4"><strong>{{ $fp->name }}</strong><p class="text-slate-300">{{ $fp->description }}</p></div>@endforeach</x-card>@endforeach</div>@endforeach</section></x-layouts.app>
