<x-layouts.app title="AI Readiness Assessment"><section class="mx-auto max-w-3xl px-6 py-16"><h1 class="text-4xl font-bold">AI Readiness Assessment</h1><p class="mt-4 text-slate-300">Score each area from 1 (early) to 5 (strong).</p><form class="mt-8 grid gap-6" method="post" action="{{ route('assessment.submit') }}">@csrf<input class="rounded bg-white/10 p-3" name="name" placeholder="Name"><input class="rounded bg-white/10 p-3" name="email" type="email" placeholder="Email"><input class="rounded bg-white/10 p-3" name="company" placeholder="Company">@foreach($questions as $q)<x-card><label class="font-semibold" for="q{{ $q->id }}">{{ $q->question }}</label><p class="text-slate-300">{{ $q->help_text }}</p><select id="q{{ $q->id }}" class="mt-3 rounded bg-slate-900 p-3" name="responses[{{ $q->id }}]" required>@for($i=1;$i<=5;$i++)<option value="{{ $i }}">{{ $i }}</option>@endfor</select></x-card>@endforeach<button class="rounded bg-cyan-400 px-5 py-3 font-semibold text-slate-950">See result</button></form></section></x-layouts.app>
