<x-admin.layout heading="Assessment Submission"><x-card><dl class="grid gap-3 md:grid-cols-2"><div><dt class="text-slate-400">Submitter</dt><dd>{{ $assessment->name ?: 'Anonymous' }}</dd></div><div><dt class="text-slate-400">Email</dt><dd>{{ $assessment->email ?: '—' }}</dd></div><div><dt class="text-slate-400">Company</dt><dd>{{ $assessment->company ?: '—' }}</dd></div><div><dt class="text-slate-400">Submitted</dt><dd>{{ $assessment->created_at?->format('M j, Y g:ia') }}</dd></div><div><dt class="text-slate-400">Score</dt><dd>{{ $assessment->score }}</dd></div><div><dt class="text-slate-400">Tier</dt><dd>{{ $assessment->result_tier }}</dd></div></dl><h2 class="mt-6 text-xl font-semibold">Recommendation</h2><p class="mt-2 text-slate-300">{{ $assessment->summary ?: 'No recommendation saved.' }}</p></x-card><h2 class="mt-8 text-2xl font-semibold">Responses</h2><div class="mt-4 space-y-3">@foreach($assessment->responses as $response)<div class="rounded border border-slate-700 p-4"><p class="font-semibold">{{ $response->question?->question ?: 'Question removed' }}</p><p class="text-sm text-slate-400">{{ $response->question?->category ?: 'Uncategorized' }}</p><p class="mt-2">Score: {{ $response->score }}</p>@if($response->answer)<p class="mt-2 text-slate-300">{{ $response->answer }}</p>@endif</div>@endforeach</div></x-admin.layout>
