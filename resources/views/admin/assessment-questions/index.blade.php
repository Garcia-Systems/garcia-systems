<x-admin.layout heading="Assessment Questions"><div class="mb-6 flex justify-between"><a class="rounded bg-cyan-400 px-4 py-2 text-slate-950" href="{{ route('admin.assessment-questions.create') }}">Create question</a><a class="text-cyan-300" href="{{ route('admin.assessment-submissions.index') }}">Review submissions</a></div><form method="post" action="{{ route('admin.assessment-questions.reorder') }}">@csrf @method('patch')<div class="space-y-3">@foreach($questions as $question)<div class="rounded border border-slate-700 p-4"><div class="flex gap-4"><input class="w-20 text-slate-900" type="number" min="0" name="orders[{{ $question->id }}]" value="{{ $question->sort_order }}"><div class="flex-1"><a class="font-semibold text-cyan-300" href="{{ route('admin.assessment-questions.edit', $question) }}">{{ $question->question }}</a><p class="text-sm text-slate-400">{{ $question->category ?: 'Uncategorized' }} · Weight {{ $question->weight }} · {{ $question->is_active ? 'Active' : 'Inactive' }}</p></div></div></div>@endforeach</div><button class="mt-4 rounded bg-cyan-400 px-4 py-2 text-slate-950">Save order</button></form><div class="mt-6">{{ $questions->links() }}</div></x-admin.layout>
