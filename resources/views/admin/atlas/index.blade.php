<x-admin.layout :title="$config['label']" :heading="'Atlas / '.$config['label']">
    <div class="mb-4 flex flex-wrap gap-3 text-sm">@foreach($resources as $key => $meta)<a class="rounded border border-white/10 px-3 py-1 @if($key === $resource) bg-white/10 text-cyan-200 @endif" href="{{ route('admin.atlas.index', $key) }}">{{ $meta['label'] }}</a>@endforeach</div>
    <div class="mb-6 flex flex-wrap items-center justify-between gap-3"><p class="text-sm text-slate-300">Manage Opportunity Atlas {{ strtolower($config['label']) }}.</p><a class="rounded bg-cyan-400 px-4 py-2 text-sm font-semibold text-slate-950" href="{{ route('admin.atlas.create', $resource) }}">New {{ $config['singular'] }}</a></div>
    <div class="overflow-hidden rounded-xl border border-white/10"><table class="w-full text-left text-sm"><thead class="bg-white/10 text-slate-200"><tr><th class="p-3">Name</th><th class="p-3">Slug</th><th class="p-3">Relationships</th><th class="p-3 text-right">Actions</th></tr></thead><tbody class="divide-y divide-white/10">
    @foreach($items as $item)<tr><td class="p-3 font-medium">{{ $item->name }}</td><td class="p-3 text-slate-300">{{ $item->slug }}</td><td class="p-3 text-slate-300">@if($item->industry)Industry: {{ $item->industry->name }} @endif @if($item->companyType)Company type: {{ $item->companyType->name }} @endif @if($item->department)Department: {{ $item->department->name }} @endif @if($item->workflow)Workflow: {{ $item->workflow->name }} @endif @if($item->solutionPatterns ?? false)Patterns: {{ $item->solutionPatterns->pluck('name')->join(', ') }} @endif @if($item->capabilities ?? false)Capabilities: {{ $item->capabilities->pluck('name')->join(', ') }} @endif</td><td class="p-3 text-right"><a class="text-cyan-300" href="{{ route('admin.atlas.edit', [$resource, $item]) }}">Edit</a></td></tr>@endforeach
    </tbody></table></div><div class="mt-6">{{ $items->links() }}</div>
</x-admin.layout>
