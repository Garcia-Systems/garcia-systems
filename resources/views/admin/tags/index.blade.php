<x-admin.layout heading="Tags"><form class="mb-6 grid gap-3" method="post" action="{{ route('admin.tags.store') }}">@csrf<input class="text-slate-900" name="name" placeholder="Name" required><input class="text-slate-900" name="slug" placeholder="Slug"><button class="rounded bg-cyan-400 px-4 py-2 text-slate-950">Add tag</button></form>@foreach($tags as $tag)<div class="flex items-center justify-between border-t border-slate-700 py-2"><span>{{ $tag->name }} <span class="text-slate-400">/{{ $tag->slug }} · {{ $tag->articles_count }} articles</span></span><div class="flex gap-3"><a class="text-cyan-300" href="{{ route('admin.tags.edit', $tag) }}">Edit</a><form method="post" action="{{ route('admin.tags.destroy', $tag) }}">@csrf @method('delete')<button class="text-red-300">Delete</button></form></div></div>@endforeach{{ $tags->links() }}</x-admin.layout>
