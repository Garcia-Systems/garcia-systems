<x-admin.layout heading="Tags"><form class="mb-6 grid gap-3" method="post" action="{{ route('admin.tags.store') }}">@csrf<input class="text-slate-900" name="name" placeholder="Name" required><input class="text-slate-900" name="slug" placeholder="Slug"><button class="rounded bg-cyan-400 px-4 py-2 text-slate-950">Add tag</button></form>@foreach($tags as $tag)<div class="border-t border-slate-700 py-2">{{ $tag->name }} <span class="text-slate-400">/{{ $tag->slug }}</span></div>@endforeach{{ $tags->links() }}</x-admin.layout>
