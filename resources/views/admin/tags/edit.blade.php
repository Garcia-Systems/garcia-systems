<x-admin.layout heading="Edit tag"><form class="grid gap-3" method="post" action="{{ route('admin.tags.update', $tag) }}">@csrf @method('put')<input class="text-slate-900" name="name" value="{{ old('name', $tag->name) }}" required><input class="text-slate-900" name="slug" value="{{ old('slug', $tag->slug) }}"><p class="text-sm text-slate-400">Related articles: {{ $tag->articles_count }}</p><button class="rounded bg-cyan-400 px-4 py-2 text-slate-950">Update tag</button></form></x-admin.layout>
