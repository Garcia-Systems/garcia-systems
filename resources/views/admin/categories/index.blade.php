<x-admin.layout heading="Categories"><form class="mb-6 grid gap-3" method="post" action="{{ route('admin.categories.store') }}">@csrf<input class="text-slate-900" name="name" placeholder="Name" required><input class="text-slate-900" name="slug" placeholder="Slug"><textarea class="text-slate-900" name="description" placeholder="Description"></textarea><button class="rounded bg-cyan-400 px-4 py-2 text-slate-950">Add category</button></form>@foreach($categories as $category)<div class="border-t border-slate-700 py-2">{{ $category->name }} <span class="text-slate-400">/{{ $category->slug }}</span></div>@endforeach{{ $categories->links() }}</x-admin.layout>
