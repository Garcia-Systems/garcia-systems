<x-admin.layout heading="Edit category"><form class="grid gap-3" method="post" action="{{ route('admin.categories.update', $category) }}">@csrf @method('put')<input class="text-slate-900" name="name" value="{{ old('name', $category->name) }}" required><input class="text-slate-900" name="slug" value="{{ old('slug', $category->slug) }}"><textarea class="text-slate-900" name="description">{{ old('description', $category->description) }}</textarea><p class="text-sm text-slate-400">Related articles: {{ $category->articles_count }}</p><button class="rounded bg-cyan-400 px-4 py-2 text-slate-950">Update category</button></form></x-admin.layout>
